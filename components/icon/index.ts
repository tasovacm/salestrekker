import { createIconSetFromIcoMoon } from "react-native-vector-icons";
import * as trekkerConfig from "../../assets/resources/fonts/config.json";

const Icon = createIconSetFromIcoMoon(trekkerConfig);

export default Icon;
