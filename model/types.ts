type formatObject= {
    value:number,
    formatted: string,
}

type Lender = {
    id: string,
    uuid?: string,
    abbr?: string,
    name: string,
    description?: string,
    type?: [string],
    group?: string,

}
